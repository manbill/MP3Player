<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center">

    <ProgressBar
        android:id="@+id/loading_progress_bar"
        android:layout_width="@dimen/fragment_loading_progress_bar_width"
        android:layout_height="@dimen/fragment_loading_progress_bar_height"
        android:layout_centerHorizontal="true"
        style="?android:progressBarStyleLargeInverse" />

    <TextView
        android:id="@+id/loading_hint_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/loading_progress_bar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/fragment_loading_hint_text_top_margin"
        android:textSize="@dimen/fragment_loading_hint_text_size"
        android:text="@string/fragment_loading_hint_text" />

</RelativeLayout>